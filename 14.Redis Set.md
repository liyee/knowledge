# Redis Set
SET数据在 Redis 中会创建一个字符串（String）类型的键值对。  

一个 Redis 字符串对象的内存占用主要由三部分组成：  
 - **redisObject 结构体**：这是所有 Redis 对象的 “头”，用于标识对象的类型、编码等元信息。  
 - **SDS 结构体**：Redis 用于存储字符串的自定义数据结构（Simple Dynamic String）。  
 - **实际的字符串数据**：你存入的数字 uint64（1454515918027264） 本身。 
 
1. **redisObject 结构体 (16 字节)**  
    在 64 位系统中，一个redisObject结构体的大小是16 字节。它的定义简化如下：  
    ```
    struct redisObject { 
        unsigned type:4;        // 对象类型 (4 bits)
        unsigned encoding:4;    // 编码方式 (4 bits)
        unsigned lru:LRU_BITS;  // LRU 时间戳 (24 bits)
        int refcount;           // 引用计数 (4 bytes)
        void *ptr;              // 指向实际数据的指针 (8 bytes)
    };
    ```
    - type 和 encoding 各占 4 位，总共 1 字节。
    - lru 占 24 位，即 3 字节。
    - refcount 是一个 4 字节的整数。
    - ptr 是一个 8 字节的指针。
    - 总计：1 + 3 + 4 + 8 = 16 字节。

2. **SDS 结构体 (40 字节)**  
SDS 的全称是 Simple Dynamic String，它是 Redis 底层存储字符串的核心结构。一个 SDS 结构体的大小也是 40 字节 (在 64 位系统中)。  
    ```
    struct sdshdr8 {
        uint8_t len;        // 字符串的长度 (1 byte)
        uint8_t alloc;      // 已分配的内存长度 (1 byte)
        unsigned char flags;// 标志位，用于标识 SDS 的类型 (1 byte)
        char buf[];         // 柔性数组，存储字符串的实际内容
    };
    ```
    - len: 记录了字符串的长度。对于 "1454515918027264" 这个字符串，它的长度是 16。所以 len 的值是 16。
    - alloc: 记录了为 buf 分配的总字节数。当字符串长度小于 1MB 时，alloc 通常等于 len。当字符串长度超过 1MB 时，Redis 会多分配一些空间以减少频繁的内存重分配。这里 alloc 的值也是 16。
    - flags: 标识了 SDS 的类型（如 sdshdr8, sdshdr16 等），占 1 字节。
    - buf[]: 这是一个柔性数组，它本身不占结构体的大小，但其内容占用的内存是计算在内的。
    - 结构体本身大小：1 + 1 + 1 = 3 字节。
     - 注意： 在 64 位系统中，结构体的大小会进行内存对齐（Padding），以提高访问效率。sdshdr8 实际占用的空间是 40 字节（这是 Redis 内部的一个固定大小，包含了 buf 数组的部分预分配空间）。

3. **实际的字符串数据 (16 字节)**  
你存入的数字 1454515918027264 被 Redis 当作一个字符串来存储。我们需要计算这个字符串的字节长度。

- 数字 1454515918027264 转换成字符串是 "1454515918027264"。
- 我们来数一数它的字符个数：1 4 5 4 5 1 5 9 1 8 0 2 7 2 6 4，一共是 16 个字符。
- 在 Redis 中，字符串默认使用 UTF-8 编码，每个英文字母和数字都占用 1 字节。
- 因此，这部分的大小是 16 字节。
- 注意： C 语言中的字符串是以 \0 结尾的，但 Redis 的 SDS 并不需要这个结束符，它的长度由 len 字段决定。

**总内存占用计算**  
现在，我们把这三部分加起来：  
- redisObject 结构体：16 字节
- SDS 结构体：40 字节
- 实际字符串数据：16 字节
- 总计：16 + 40 + 16 = 72 字节 

**不同版本有差异总结：**
- Redis 版本差异：不同版本的 Redis 在 redisObject 和 SDS 的结构上可能有细微差别。
- 内存对齐和填充：在某些情况下，为了满足 CPU 的内存对齐要求，编译器会在结构体成员之间或末尾添加额外的 “填充” 字节。
- jemalloc 的 size_classes：jemalloc 的分配大小是基于一套预设的 size_classes。对于较小的对象，可能的分配大小是 8, 16, 32, 48, 64, 80, 96, 112, 128, ... 等。