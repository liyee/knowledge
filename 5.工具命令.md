# 工具命令
## 1. proto生成protobuf文件
   protoc --proto_path=proto --go_out=./ --go_opt=paths=source_relative user.proto
## 2. kubenetes操作
### 2.1. 基础
    1.kubenetes默认空间设置：kubectl config set-context --current --namespace=jspp
    2.查看pob信息：kubectl -n jspp get pod (-n 命名空间)
    kubectl get pods --sort-by=.metadata.creationTimestamp（按创建时间排序）
    3.查看pod日志数据：
    kubectl logs -f deploy/netsecuritydatareportsv netsecuritydatareportsv --tail 10 
    kubectl logs -f deploy/usersv usersv --tail 10 -n jspp（命名空间）
    4.进入pod： kubectl exec --stdin --tty netsecuritydatareportsv-d9cf6df85-x2xg7 -c netsecuritydatareportsv -- /bin/sh
    5.进入mysql(pod)：kubectl exec --stdin --tty mysql-0 -- /bin/sh
    6.创建对象：kubectl apply -f ./my-manifest.yaml
    7.删除指定的Pod：kubectl delete pod <pod-name>

### 2.2. 发布
    1.查看发布历史：kubectl rollout history deployment <deployment-name>
    2.查看回滚状态：kubectl rollout status deployment <deployment-name>
    3.回滚到上一个版本：kubectl rollout undo deployment <deployment-name>
    4.回滚到指定版本：kubectl rollout undo deployment <deployment-name> --to-revision=<revision-number>

### 2.3. 多集群
    1.查看所有集群：kubectl config get-clusters
    2.查看所有用户：kubectl config get-users
    3.查看所有上下文（核心，切换的是上下文）：kubectl config get-contexts
    4.切换上下文：kubectl config use-context [上下文名称]
    5. 手动打包上传：
    GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o pod *
    tar -czvf pod.tar.gz pod
    kubectl cp -c pod pod.tar.gz pod-xx:/tmp/
    tar -xzvf pod.tar.gz
    /tmp/pod retentionData --config /root/.pod.yaml

### 2.4. 文件拷贝
    1. 拷贝Pod内单个文件到本地
    kubectl cp <命名空间>/<Pod名称>:<Pod内文件路径> <本地目标路径>

    2. 拷贝Pod内整个目录到本地（目录需加 / 结尾，或本地路径也加 / 结尾）
    kubectl cp <命名空间>/<Pod名称>:<Pod内目录路径>/ <本地目标目录>/

    3. 直接读取Pod内文件内容，重定向到本地（无需tar）
    kubectl exec -n bff my-app-7f987d689d-2xq8z -- cat /app/large_file.tar > ~/large_file.tar

    4. 拷贝本地单个文件到Pod内指定路径
    kubectl cp <本地文件路径> <命名空间>/<Pod名称>:<Pod内目标路径>

    5. 拷贝本地整个目录到Pod内指定目录
    kubectl cp <本地目录路径>/ <命名空间>/<Pod名称>:<Pod内目标目录>/

    6. 进入 Pod 终端（指定容器和命名空间）
    kubectl exec -it -c pop -n bff pop-4s4d4 -- /bin/bash

    7. 在 Pod 内打包 /usr/local/bin/app/ 目录为 app.tar.gz
    tar -czvf /tmp/app.tar.gz /usr/local/bin/app/

    8. 退出 Pod（按 Ctrl+D），拷贝打包后的文件到本地
    kubectl cp -c pop -n bff pop-4s4d4:/tmp/app.tar.gz ~/Downloads/app.tar.gz

## 3. 服务映射
### 3.1. 开启：
    nohup kubectl port-forward services/mysql-svc 3306:3306 -n jspp >> log/log-$(date +"%Y%m%d").log 2>&1 &
    nohup kubectl port-forward services/mongo-svc 27017:27017 -n jspp >> log/log-$(date +"%Y%m%d").log 2>&1 &
    nohup kubectl port-forward services/redis-svc 6379:6379 -n jspp >> log/log-$(date +"%Y%m%d").log 2>&1 &
    nohup kubectl port-forward services/usersv-svc 9090:9090 8080:8080 -n jspp >> log/log-$(date +"%Y%m%d").log 2>&1 &
    nohup kubectl port-forward services/authsv-svc 9091:9090 8081:8080 -n jspp >> log/log-$(date +"%Y%m%d").log 2>&1 &
    nohup kubectl port-forward services/groupsv-svc 9092:9090 -n jspp >> log/log-$(date +"%Y%m%d").log 2>&1 &
    nohup kubectl port-forward services/messagesv-svc 9093:9090 8083:8080 -n jspp >> log/log-$(date +"%Y%m%d").log 2>&1 &

### 3.2. 关闭
    ps aux| grep port-forward| grep -v grep| awk '{print $2}'| xargs kill -9

### 3.3. 探测服务端口是否使用
    nc -z -w 1 "127.0.0.1" "8080" &>/dev/null

## 4. go项目配置
    go env -w GOPRIVATE='code.jspp.com'
    go env -w GONOPROXY='code.jspp.com'
    go env -w GONOSUMDB='code.jspp.com'

## 5. git操作
### 5.1. 配置全局提交模板
    git config --global commit.template ~/.git-global-template/.commit-template 
    git config --global init.templateDir ~/.git-global-template

### 5.2. commit.template的内容
    # feat：新功能（feature） 
    # fix：修补bug # docs：文档（documentation） 
    # style： 格式（不影响代码运行的变动） 
    # refactor：重构（即不是新增功能，也不是修改bug的代码变动） 
    # test：增加测试 
    # chore：构建过程或辅助工具的变动

### 5.3. 基本命令
    查看配置：cat .gitconfig
    查看分支：git config --global alias.tree 'log --oneline --graph --all -15'
    临时存储：git stash save "test-cmd-stash”（git stash 添加别名）
    替换路径：git config --global url."git@code.jspp.com".insteadOf "https://code.jspp.com"
    忽略已提交的文件：git rm --cached *
    回滚： git reset --hard <目标版本号>， git push -f origin <branch>
    回退文件到某个版本：git restore --source=<commit_id> <file_path>
    放弃本地添加的文件：git clean -xdf [x-包括忽略的文件;X-仅删除忽略的文件;d-包含目录;f-强制删除]
    放弃本地修改的文件：git checkout .
    删除本地分支：git branch -d feature-branch
    删除远程分子：git push origin --delete remote_branch_name
    只合并单个提交：git cherry-pick <commit-id>
    合并上一次提交: git commit --amend
    查看暂存区更改: git diff --cached
    查看某一个分支的提交: git log feature-branch
    查看某一个文件的提交: git log -p -1 file
    查看所有远程分支: git branch -r
    查看本地和远程分支: git branch -a
    更新分支名称: git branch -m 新分支名｜git branch -m 旧分支名 新分支名
    某次提交创建新分支：git branch 新分支名 commit-id

### 5.4. stash使用
    存储未跟踪的文件： git stash -u
    存储被版本库忽略的文件： git stash -a
    git stash添加一个注释： git stash save "备注信息"
    查看堆栈中的 stash 列表：git stash list
    查看特定 stash 的内容：git stash show -p stash@{id}
    将堆栈中的某个 stash 应用到工作区：git stash apply stash@{id}
    成功应用了堆栈的 stash 后，立即将其从堆栈中删除：git stash pop
    删除堆栈中的特定 stash：git stash drop stash@{id}
    删除所有的 stash：git stash clear

### 5.5. tag 使用
    //创建轻量标签
    git tag 标签名
    git tag 标签名 提交版本号

    //创建附注标签
    git tag -a 标签名 -m "标签说明"
    git tag -a 标签名 提交版本号 -m "标签说明"

    git tag //列出所有标签
    git tag -l "v1.*" //按模式筛选标签
    git show 标签名 //查看标签详细信息
    git tag -d 标签名 //删除本地标签
    git push origin --delete 标签名 //删除远程标签
    git push origin 标签名 //推送单个标签
    git push origin --tags //推送所有标签

    //检出标签
    git checkout 标签名
    git checkout -b 分支名 标签名

## 6. go增加配置文件执行
    go run main.go run --config config.yaml  or  go run main.go run --config=config.yaml
    Viscode配置调试："args": ["registerUserdataReport","--config","xxx/config.yaml"],

## 7. [nvm安装](https://nvm.uihtm.com/doc/guide.html)
    brew install nvm
    curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash

## 8. Mysql 8.0.39 安装
    1. docker run -itd --name mysql -p 3307:3306 -e MYSQL_ROOT_PASSWORD=123456789 mysql:8.0.39
    2. 控制台查询乱码：
    show create database <jspp>;
    show variables like 'character%';
    set names <utf8mb4>;
    3.显示表详细结构：show create table t_abnormal_user;

    解决中文乱码的原理就是，在执行SQL语句之前，将MySQL以下三个系统参数设置为与服务器字符集character-set-server相同的字符集。
    set character_set_client=gbk;   客户端的字符集。
    set character_set_connection=gbk;   连接字符集
    set character_set_results=gbk;   结果字符集。
    可以直接用set names gbk;替代上面三个执行命令

    查看mysql的字符集：`show variables like '%character%';`
    查看某个数据库字符集：`show create database 数据库名;`
    查看某个数据表字符集：`show create table 表名;`
    查看某个数据表的字段字符集：`show full fields from 表名；`
    指定编码格式登录mysql：`mysql -u root -proot --default-character-set=gbk`
    修改字符集编码：`set character_set_client=gbk;`
    修改数据库的字符集：`alter database 数据库名 character set gbk;`
    修改数据表的字符集：`alter table 表名 character set gbk;`
    修改字段的字符集:`alter table 表名 change 字段名 字段名 char(10) character set utf-8;`

    1、UTF-8包含全世界所有国家需要用到的字符，是国际编码，通用性强。它对英文使用8位（即一个字节），中文使用24位（三个字节）来编码。UTF-8编码的文字可以在各国支持UTF8字符集的浏览器上显示，即在外国人的英文IE上也能显示中文，无需下载IE的中文语言支持包。
    2、GBK是国家标准GB2312基础上扩容后兼容GB2312的标准。GBK的文字编码是用双字节来表示的，即不论中、英文字符均使用双字节来表示，为了区分中文，将其最高位都设定成1。GBK包含全部中文字符，是国家编码，通用性比UTF8差，不过UTF8占用的数据库比GBK大。
    3、GB2312是GBK的子集，GBK是GB18030的子集。GBK是包括中日韩字符的大字符集合，如果是中文的网站，推荐GB2312 ，GBK有时还是有点问题。
    4、为了避免所有乱码问题，应该采用UTF-8，将来要支持国际化也非常方便，UTF-8可以看作是大字符集，它包含了大部分文字的编码。使用UTF-8的一个好处是其他地区的用户（如香港台湾）无需安装简体中文支持就能正常观看文字而不会出现乱码。

    gb2312是简体中文的码
    gbk支持简体中文及繁体中文
    big5支持繁体中文
    utf-8支持几乎所有字符

    character-set-server/default-character-set：服务器字符集，默认情况下所采用的。
    character-set-database：数据库字符集。
    character-set-table：数据库表字符集。
    优先级依次增加。所以一般情况下只需要设置character-set-server，而在创建数据库和表时不特别指定字符集，这样统一采用character-set-server字符集。
    character-set-client：客户端的字符集。客户端默认字符集。当客户端向服务器发送请求时，请求以该字符集进行编码。
    character-set-results：结果字符集。服务器向客户端返回结果或者信息时，结果以该字符集进行编码。
    在客户端，如果没有定义character-set-results，则采用character-set-client字符集作为默认的字符集。所以只需要设置character-set-client字符集。
    要处理中文，则可以将character-set-server和character-set-client均设置为GB2312，如果要同时处理多国语言，则设置为UTF8。

## 9. 设置npm国内源
    #查询当前使用的镜像源：npm get registry
    #设置为淘宝镜像源：npm config set registry https://registry.npmmirror.com/
    #设置为腾讯镜像源：npm config set registry https://mirrors.cloud.tencent.com/npm/
    #某个包使用腾讯源：npm install [package] --registry=https://mirrors.cloud.tencent.com/npm/
    #验证设置：npm get registry
    #还原为官方源：npm config set registry https://registry.npmjs.org/
    #查看配置信息：npm config list
    #通过某个源加载：npm --registry=https://mirrors.cloud.tencent.com/npm install
    #设置源不生效：替换项目 package-lock.json 文件的源

## 10. react-redux 创建项目
    npx create-react-app app01 --template redux-typescript 

## 11. linux命令
    压缩包：tar -jcvf jspp.tar.bz2 --exclude=admin-website/node_modules --exclude=0725 --exclude=bin --exclude=test --exclude=umi jspp
    更新文件内容：sed -i '' 's#/usr/local/bin/i18n#/data/i18n#g' Dockerfile //mac -i '' 否则报错

    linux图片处理：ImageMagick
    文件按大小排序：ls -lS
    去除重复文件：fdupes
                find dist/bff_images -type f -name "*.png" | xargs md5 | sort | uniq -d -w 32
    curl重复请求：for i in {1..15}; do curl -H "Content-Type: application/json" -d '{"user_id":100}' http://127.0.0.1:8092/time-consume/push;echo ""; done
    curl --retry 4 --retry-delay 1 http://example.com
    查看端口号1：netstat -tunlp | grep 8000
    查看端口号2：lsof -i:端口号

    windows 系统中 通过 route print 命令查看本地的路由配置
    linux 查看路由信息 route -n

    window 中使用 tracert 查看 访问 114.114.114.114: tracert 114.114.114.114
    linux 中 使用 traceroute 查看访问 114.114.114.114 时经过的路由信息: traceroute 114.114.114.114
    安装yum install -y traceroute

    参考：https://developer.aliyun.com/article/1573518

## 12. [SM2在线工具](https://the-x.cn/zh-cn/cryptography/Sm2.aspx)
- [ASN.1 在线解码工具](https://the-x.cn/encodings/Asn1.aspx)
- [在线国密SM2加密/解密](https://the-x.cn/cryptography/Sm2.aspx)
- [SM2密钥对生成器](https://www.ufreetools.com/zh/tool/sm2-key-pair-generator)

## 13. sftp命令
    mac下载：brew install openssh-client
    linux下载：sudo apt-get instal openssh-client

    建立连接：sftp username@server_ip_address
            sftp [-P port] username@server_ip_address
    上传：put [-r] local_file_path [remote_file_path]
    下载：get [-r] remote_file_path [local_file_path]
    退出：bye

## 14. mac快捷键
    显示隐藏文件及文件夹：⌘ Command (⌘) + ⇧ Shift + .
    VsCode快捷键：
    折叠所在文件所有：Ctrl+k+0
    展开所在文件所有：Ctrl+k+j
    折叠光标所处代码块：Ctrl+Shift+[
    展开光标所处代码块：Ctrl+Shift+]
    文件列表：command+p
    方法列表：command+shift+o

## 15. nginx操作
    启动nginx：sudo nginx
    停止nginx：sudo nginx -s stop
    重启nginx：sudo nginx -s reload
    查看状态：sudo systemctl status nginx
    brew services start nginx	启动 Nginx 并设置开机自启
    brew services stop nginx	停止 Nginx 并关闭开机自启
    brew services restart nginx	重启 Nginx
    nginx	直接启动 Nginx（不设置开机自启）
    nginx -s stop	强制停止 Nginx
    nginx -s quit	优雅停止 Nginx（处理完当前请求后停止）
    nginx -s reload	重新加载配置文件（无需重启服务）
    nginx -t	测试配置文件语法是否正确

    安装：brew search nginx && brew install nginx
    配置信息：brew info nginx
    是否启动：ps -ef| grep nginx
    查看安装目录：open /usr/local/etc/nginx/

    Docroot is: /usr/local/var/www
    default port has been set in /usr/local/etc/nginx/nginx.conf to 8080
    nginx will load all files in /usr/local/etc/nginx/servers/

    https://nginx.mosong.cc/
    https://nginx.org/

## 16. apache操作
    mac：
    检查服务：httpd -v
    启动服务：sudo apachectl start
    停止服务：sudo apachectl stop
    重启服务：sudo apachectl restart
    查看状态：sudo apachectl status

    配置文件地址：/etc/apache2/httpd.conf
    默认项目目录：/Library/WebServer/Documents/

## 17. 阿里云日志服务
    content: CheckRiskControl and content: "actions_map:map[8:0]" | SELECT distinct regexp_extract("content", 'user_id:(\d+)', 1) as user_id FROM log LIMIT 10000